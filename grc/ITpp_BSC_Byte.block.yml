# auto-generated by grc.converter

id: ITpp_BSC_Byte
label: BSC Byte
category: '[ITpp]'

parameters:
-   id: probability
    label: Probabilidade Bit Erro
    dtype: float
-   id: bits
    label: Qtd. bits da entrada
    dtype: int

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

asserts:
    - ${ probability >= 0 }

templates:
    imports: import ITpp
    make: ITpp.BSC_Byte(${bits}, ${probability})
    callbacks:
    - set_p(${probability})

file_format: 1
