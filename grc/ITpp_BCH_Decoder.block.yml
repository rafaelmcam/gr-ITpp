# auto-generated by grc.converter

id: ITpp_BCH_Decoder
label: BCH Decoder
category: '[ITpp]'

parameters:
-   id: n
    label: "Par\xE2metro N"
    dtype: int
-   id: t
    label: "Par\xE2metro T"
    dtype: int

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import ITpp
    make: ITpp.BCH_Decoder(${n}, ${t})

file_format: 1
